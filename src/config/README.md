# config

Purpose: App configuration, constants, and environment accessors.

Notes:
- Keep non-secret constants here (e.g., THEME, FEATURE_FLAGS).
- Read environment vars via import.meta.env in one place.
