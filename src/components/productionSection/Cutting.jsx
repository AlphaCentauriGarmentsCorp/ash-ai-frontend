import React, { useState } from "react";

const Cutting = ({ order }) => {
  return (
    <section className="flex flex-col gap-y-5">
      <h1 className="font-semibold text-lg">Cutting</h1>
    </section>
  );
};

export default Cutting;
