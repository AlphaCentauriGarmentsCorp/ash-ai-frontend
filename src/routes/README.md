# routes

Purpose: Centralize routing logic and helpers.

Notes:
- Place ProtectedRoute/PublicRoute wrappers, route configs, and lazy loaders.
- Keep page-level routes in src/pages but import configs from here if desired.
