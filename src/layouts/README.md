# layouts

Purpose: Layout components providing shared structure (headers, sidebars, footers).

Notes:
- Keep cross-page wrappers here.
- Current components are still under components/Layout, re-exported via index.js for gradual migration.
